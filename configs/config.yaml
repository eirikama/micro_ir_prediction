# configs/config.yaml
defaults:
  - model: aacnn
  - data: default
  - trainer: default
  - inference: default
  - _self_

mlflow:
  tracking_uri:    "sqlite:////app/mlflow.db"
  experiment_name: "microplastics_raw"

mode: all
seed: 42

project_name: "microplastics_classification"
